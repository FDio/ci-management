ARG BASE_IMAGE
FROM ${BASE_IMAGE}

ARG DOCKER_GHA_RUNNER_DIR
ENV DOCKER_GHA_RUNNER_DIR=${DOCKER_GHA_RUNNER_DIR}
ENV PATH=$PATH:${DOCKER_GHA_RUNNER_DIR}
USER root

WORKDIR ${DOCKER_GHA_RUNNER_DIR}
RUN chmod +x entrypoint.sh
ENTRYPOINT ["entrypoint.sh"]
