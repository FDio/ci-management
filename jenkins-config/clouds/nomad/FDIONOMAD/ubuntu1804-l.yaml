templates:
  - constraints:
      - ltarget: "^${attr.cpu.arch}"
        operand: "="
        rtarget: "amd64"
      - ltarget: "^${node.class}"
        operand: "="
        rtarget: "builder"
    cpu: 20000
    disk: 3000
    forcePull: false
    image: "snergster/vpp-ubuntu18"
    labels: "ubuntu1804-l"
    memory: 32000
    prefix: "prod-amd"
