templates:
  - constraints:
      - ltarget: "^${node.class}"
        operand: "="
        rtarget: "csit"
      - ltarget: "^${attr.cpu.arch}"
        operand: "="
        rtarget: "amd64"
    cpu: 10000
    image: "snergster/vpp-ubuntu18"
    labels: "vpp-csit-device vpp-csit-ubuntu18"
    memory: 18000
    prefix: "prod-csit"
